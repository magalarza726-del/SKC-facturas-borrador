import{store}from'../store.js';import{sync}from'../sync.js';import{prepareFileRecords}from'../files.js';import{bytesLabel,dateLabel,escapeHtml,money,todayIso}from'../utils.js';import{$,$all,badge,optionsHtml,pageHeader,promptDialog,toast}from'../ui.js';
let tab='inbox',files=[];const filtered=()=>tab==='inbox'?store.incomingMessages():tab==='outbox'?store.outgoingMessages():tab==='transfers'?store.state.messages.filter(x=>x.messageType==='TRANSFERENCIA'):store.state.messages;
function rows(){const u=store.currentUser(),rs=filtered();return rs.length?rs.map(x=>{const incoming=x.recipientUserId===u.id,act=x.messageType==='TRANSFERENCIA'&&incoming&&['ENVIADO','RECIBIDO'].includes(x.status);return`<tr><td>${dateLabel(x.createdAt,true)}</td><td>${x.messageType==='TRANSFERENCIA'?'Transferencia':'Solicitud'}</td><td><strong>${escapeHtml(incoming?x.senderName:x.recipientName)}</strong><br><span class="small muted">${incoming?'De':'Para'} ${escapeHtml(incoming?x.senderEmail:x.recipientEmail)}</span></td><td>${money(x.amount)}</td><td>${escapeHtml(x.project||'—')}</td><td>${escapeHtml(x.reason)}</td><td>${badge(x.status)} ${badge(x.syncStatus)}</td><td class="table-actions">${act?`<button class="button button-compact" data-confirm-transfer="${x.id}">Confirmar</button><button class="button button-danger button-compact" data-reject-transfer="${x.id}">Rechazar</button>`:''}${(x.attachments||[]).map(f=>`<button class="button button-ghost button-compact" data-open-message-file="${x.id}" data-file-id="${f.id}">${escapeHtml(f.filename)}</button>`).join('')}</td></tr>`}).join(''):'<tr><td colspan="8" class="empty-state">No hay mensajes en esta vista.</td></tr>'}
function fileList(){const c=$('#transferFileList');if(!c)return;c.innerHTML=files.length?files.map((f,i)=>`<div class="file-item"><span>${escapeHtml(f.filename)} · ${bytesLabel(f.size)}</span><button type="button" class="button button-ghost button-compact" data-remove-transfer-file="${i}">Quitar</button></div>`).join(''):'<span class="small muted">No hay evidencia seleccionada.</span>';$all('[data-remove-transfer-file]',c).forEach(b=>b.addEventListener('click',()=>{files.splice(Number(b.dataset.removeTransferFile),1);fileList()}))}
export function renderMessages(app,navigate){const u=store.currentUser(),users=store.users().filter(x=>x.id!==u.id),can=u.transferEnabled||store.isAdmin();files=[];app.innerHTML=`${pageHeader('Mensajes','Envía solicitudes de monto y avisos de transferencia. El ingreso se publica solo cuando el destinatario confirma.','','Comunicación y fondos')}<section class="grid grid-2"><article class="card"><div class="card-header"><h2>Solicitar monto</h2>${badge('SOLICITUD')}</div><div class="card-body"><form id="fundRequestForm"><div class="form-grid"><div class="field span-4"><label class="required">Monto USD</label><input name="amount" type="number" min=".01" step=".01" required></div><div class="field span-4"><label>Saldo declarado</label><input name="balanceDeclared" type="number" step=".01" value="0"></div><div class="field span-4"><label>Destinatario</label><select name="recipientUserId">${optionsHtml(users,'','id','name','Finanzas / externo')}</select></div><div class="field span-6"><label>Correo externo</label><input name="recipientEmail" type="email"></div><div class="field span-6"><label>Proyecto</label><input name="project" type="text"></div><div class="field"><label class="required">Motivo</label><textarea name="reason" required></textarea></div></div><div class="form-actions"><button class="button" type="submit">Guardar solicitud</button></div></form></div></article><article class="card"><div class="card-header"><h2>Avisar transferencia</h2>${can?badge('HABILITADO'):badge('BLOQUEADO')}</div><div class="card-body">${can?`<form id="transferForm"><div class="form-grid"><div class="field span-4"><label class="required">Monto USD</label><input name="amount" type="number" min=".01" step=".01" required></div><div class="field span-4"><label class="required">Destinatario</label><select name="recipientUserId" required>${optionsHtml(users,'','id','name','Seleccione')}</select></div><div class="field span-4"><label class="required">Fecha</label><input name="transferDate" type="date" value="${todayIso()}" required></div><div class="field span-6"><label>Referencia bancaria</label><input name="reference"></div><div class="field span-6"><label>Proyecto</label><input name="project"></div><div class="field"><label class="required">Concepto</label><textarea name="reason" required></textarea></div><div class="field"><label>Evidencia</label><div class="file-drop"><input id="transferFiles" type="file" multiple accept="image/*,application/pdf"><div><strong>Adjuntar comprobante</strong><br><span class="small muted">Máximo 20 MB por archivo.</span></div></div><div id="transferFileList" class="file-list"></div></div></div><div class="form-actions"><button class="button" type="submit">Enviar aviso</button></div></form>`:`<div class="alert alert-warning">El administrador debe habilitar transferencias para ${escapeHtml(u.name)}.</div>`}</div></article></section><section class="card" style="margin-top:18px"><div class="card-header"><h2>Bandeja de mensajes</h2><span class="small muted">Los cambios se comparten cuando Supabase está configurado.</span></div><div class="card-body" style="padding-bottom:0"><div class="tabs"><button class="tab ${tab==='inbox'?'active':''}" data-message-tab="inbox">Recibidos (${store.incomingMessages().length})</button><button class="tab ${tab==='outbox'?'active':''}" data-message-tab="outbox">Enviados (${store.outgoingMessages().length})</button><button class="tab ${tab==='transfers'?'active':''}" data-message-tab="transfers">Transferencias</button><button class="tab ${tab==='all'?'active':''}" data-message-tab="all">Todos</button></div></div><div class="table-wrap"><table><thead><tr><th>Fecha</th><th>Tipo</th><th>Contacto</th><th>Monto</th><th>Proyecto</th><th>Concepto</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>${rows()}</tbody></table></div></section>`;
 const rf=$('#fundRequestForm',app);rf.addEventListener('submit',async e=>{e.preventDefault();try{const x=await store.createFundMessage(Object.fromEntries(new FormData(rf)));toast(`${x.code} fue guardada.`,'success','Solicitud creada');renderMessages(app,navigate)}catch(err){toast(err.message,'error')}});const tf=$('#transferForm',app);if(tf){$('#transferFiles',app).addEventListener('change',async e=>{try{files.push(...await prepareFileRecords(e.target.files));fileList();e.target.value=''}catch(err){toast(err.message,'error')}});fileList();tf.addEventListener('submit',async e=>{e.preventDefault();try{const x=await store.createTransferMessage(Object.fromEntries(new FormData(tf)),files);toast(`${x.code} fue enviado a ${x.recipientName}.`,'success','Transferencia registrada');renderMessages(app,navigate)}catch(err){toast(err.message,'error')}})}
 $all('[data-message-tab]',app).forEach(b=>b.addEventListener('click',()=>{tab=b.dataset.messageTab;renderMessages(app,navigate)}));$all('[data-confirm-transfer]',app).forEach(b=>b.addEventListener('click',async()=>{try{await store.confirmTransfer(b.dataset.confirmTransfer);toast('El ingreso fue publicado en el flujo.','success','Transferencia confirmada');renderMessages(app,navigate)}catch(err){toast(err.message,'error')}}));$all('[data-reject-transfer]',app).forEach(b=>b.addEventListener('click',async()=>{const r=await promptDialog('Rechazar transferencia','Motivo del rechazo',{multiline:true,minLength:3,danger:true,submitLabel:'Rechazar'});if(r)try{await store.rejectTransfer(b.dataset.rejectTransfer,r);toast('La transferencia fue rechazada.','success');renderMessages(app,navigate)}catch(err){toast(err.message,'error')}}));$all('[data-open-message-file]',app).forEach(b=>b.addEventListener('click',async()=>{const m=store.state.messages.find(x=>x.id===b.dataset.openMessageFile),a=m?.attachments?.find(x=>x.id===b.dataset.fileId);try{if(m&&a)await sync.openAttachment('messages',m.id,a)}catch(err){toast(err.message,'error')}}));for(const x of store.incomingMessages().filter(m=>m.status==='ENVIADO'))store.markMessageReceived(x.id).catch(()=>{})}
