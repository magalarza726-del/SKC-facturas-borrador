const encoder=new TextEncoder();
const CRC_TABLE=(()=>{const t=new Uint32Array(256);for(let n=0;n<256;n+=1){let c=n;for(let k=0;k<8;k+=1)c=(c&1)?(0xedb88320^(c>>>1)):(c>>>1);t[n]=c>>>0}return t})();
const crc32=bytes=>{let c=0xffffffff;for(const b of bytes)c=CRC_TABLE[(c^b)&0xff]^(c>>>8);return(c^0xffffffff)>>>0};
const u16=(v,o,x)=>v.setUint16(o,x,true),u32=(v,o,x)=>v.setUint32(o,x>>>0,true);
function dosDateTime(date=new Date()){const y=Math.max(1980,date.getFullYear()),time=((date.getHours()&31)<<11)|((date.getMinutes()&63)<<5)|(Math.floor(date.getSeconds()/2)&31),day=((y-1980)<<9)|(((date.getMonth()+1)&15)<<5)|(date.getDate()&31);return{time,date:day}}
function concat(parts){const size=parts.reduce((s,x)=>s+x.length,0),out=new Uint8Array(size);let offset=0;for(const p of parts){out.set(p,offset);offset+=p.length}return out}
export async function createZipBlob(entries=[]){const normalized=[];for(const entry of entries){const data=entry.data instanceof Uint8Array?entry.data:new Uint8Array(await entry.data.arrayBuffer());normalized.push({name:String(entry.name||'archivo'),data})}const localParts=[],centralParts=[];let offset=0;const stamp=dosDateTime();for(const entry of normalized){const name=encoder.encode(entry.name),data=entry.data,crc=crc32(data),local=new Uint8Array(30+name.length),lv=new DataView(local.buffer);u32(lv,0,0x04034b50);u16(lv,4,20);u16(lv,6,0x0800);u16(lv,8,0);u16(lv,10,stamp.time);u16(lv,12,stamp.date);u32(lv,14,crc);u32(lv,18,data.length);u32(lv,22,data.length);u16(lv,26,name.length);u16(lv,28,0);local.set(name,30);localParts.push(local,data);const central=new Uint8Array(46+name.length),cv=new DataView(central.buffer);u32(cv,0,0x02014b50);u16(cv,4,20);u16(cv,6,20);u16(cv,8,0x0800);u16(cv,10,0);u16(cv,12,stamp.time);u16(cv,14,stamp.date);u32(cv,16,crc);u32(cv,20,data.length);u32(cv,24,data.length);u16(cv,28,name.length);u16(cv,30,0);u16(cv,32,0);u16(cv,34,0);u16(cv,36,0);u32(cv,38,0);u32(cv,42,offset);central.set(name,46);centralParts.push(central);offset+=local.length+data.length}const central=concat(centralParts),end=new Uint8Array(22),ev=new DataView(end.buffer);u32(ev,0,0x06054b50);u16(ev,4,0);u16(ev,6,0);u16(ev,8,normalized.length);u16(ev,10,normalized.length);u32(ev,12,central.length);u32(ev,16,offset);u16(ev,20,0);return new Blob([concat([...localParts,central,end])],{type:'application/zip'})}
